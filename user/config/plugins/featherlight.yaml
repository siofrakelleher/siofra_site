enabled: true                 # global enable/disable the entire plugin
active: true                 # if the plugin is active and JS/CSS should be loaded
openSpeed: 150                # open speed in ms
closeSpeed: 150               # close speed in ms
closeOnClick: anywhere      # background|anywhere|false
closeOnEsc: true              # true|false on hitting Esc key
root: body                    # where to append featherlights
